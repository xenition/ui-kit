"use strict";
/**
 * Shared data shapes for the calendar / scheduling module. These are plain,
 * serializable records the host app owns — every `Date` is passed in via props
 * so the components stay pure (no clock reads at module scope).
 */
Object.defineProperty(exports, "__esModule", { value: true });
//# sourceMappingURL=types.js.map